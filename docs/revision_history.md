# Revision History

| Date       | Changed files / modules | Summary | Related documentation updates |
|------------|--------------------------|---------|-------------------------------|
| 2026-07-12 | Phase 0 `src/` package, `.gitignore`, and tests | Added the project/package layout and git ignore rules; implemented Phase 0 domain enums, errors, parameters, models, states, and pure Protocol interfaces. Added ET-aware datetime normalization with naive-datetime rejection, `FeedEvent` validation, and unit coverage. No database, IBAPI/TWS, algorithm, or order implementation was added. | Updated the phase plan, flow, and development design documents, plus `CONTEXT`, to record the Phase 0 contracts. |
| 2026-07-12 | `engine/regression.py`, `engine/trend_engine.py`, `engine/channel_engine.py`, `engine/decision_engine.py`, `tests/test_engine.py`, and `pyproject.toml` | Implemented Phase 1's IO-free regression, Trend, Channel, and Decision algorithms. Added NumPy as a runtime dependency and unit coverage for regression validation, trend windows/classification, channel transition/prediction semantics, and Decision post-persistence state transitions. | Updated `README.md`, `CONTEXT`, and this revision history to reflect the implemented Phase 1 scope. |
| 2026-07-12 | Phase 2 persistence, feed, application, and CLI modules | Added SQLite schema/repositories, local BacktestFeed validation, atomic processed-bar/signal persistence, run summaries, and a JSON-request Backtest CLI. Raw Bar import and Raw Bar E2E tests are intentionally deferred until user-provided data is injected. | Updated README and CONTEXT for the Phase 2 offline workflow. |
| 2026-07-12 | Phase 3 `ib/`, canonical raw-bar model, SQLite storage, CLI, and tests | Replaced Phase 2 injected raw data with the canonical IBAPI BarData shape (UTC epoch date, OHLC, volume, WAP, and bar count). Added callback-correlated TWS gateway, schedule and historical-bar services, local-cache-first fetch flow, and one-time legacy-schema rebuild with later fail-fast detection. | Updated README, CONTEXT, Phase 3 plan/development documentation, and this revision history. |
| 2026-07-12 | `configs/ib.yaml`, IB configuration loader, and run CLI | Replaced environment-variable TWS connection configuration with YAML paper/live profiles selected by the CLI. | Updated README, CONTEXT, and this revision history. |
| 2026-07-12 | IBAPI dependency and gateway version guard | Replaced the obsolete PyPI ibapi 9.81 dependency with the official IBKR API requirement and added a fail-fast protocol check for SCHEDULE support. | Updated README, CONTEXT, and this revision history. |
| 2026-07-12 | `IbApiGateway.error` callback | Updated the error callback to the official API 10.48 signature, including `errorTime`, so TWS errors are converted into domain errors instead of terminating the event thread. | Updated this revision history. |
| 2026-07-12 | SQLite processed-bar persistence | Corrected the processed-bar INSERT placeholder count to match the Phase 3 table schema. | Updated this revision history. |
| 2026-07-12 | `configs/parameter_set.csv`, parameter loader, and run request format | Centralized parameter definitions in CSV and reduced request JSON to a CSV path plus `parameter_set_id`; embedded parameter dictionaries are no longer accepted. | Updated README, CONTEXT, and this revision history. |
| 2026-07-12 | `persistence/database.py` | Corrected the processed-bar INSERT placeholder count discovered during the first injected-data Backtest. The failed transaction wrote no processed bars or signals. | Revision history updated with the production validation correction. |
| 2026-07-12 | `persistence/database.py`, SQLite `raw_1m_bar` and `processed_1m_bar` | Expanded persisted Trend, Channel, and Decision result JSON fields into queryable columns, retained JSON for compatibility, and added Raw Bar source fields `bar_size`, `what_to_show`, and `use_rth`. Existing processed rows are backfilled during schema initialization. | Revision history updated for the schema migration. |
| 2026-07-12 | `persistence/database.py`, SQLite timestamp columns | Renamed persisted `timestamp_et` fields to `timestamp`; timestamp values remain ET-normalized by the domain layer. Existing database tables are migrated in place during initialization. | Revision history updated for the timestamp naming convention. |
| 2026-07-12 | `persistence/database.py`, SQLite `processed_1m_bar` | Added Raw Bar source metadata columns to processed bars, backfilled existing runs from raw storage, and removed redundant Trend/Channel/Decision JSON columns after their values had been expanded. | Updated CONTEXT and revision history for the normalized persisted shape. |
| 2026-07-12 | `domain/models.py`, `application/bar_processor.py`, `persistence/database.py`, `tests/test_phase3_ibapi.py` | Rebuilt `processed_1m_bar` as Phase 3 schema v2: preserved all RawBar fields including `wap` and `barCount`, expanded parameter/Trend/Channel/Decision payloads into dedicated columns, removed JSON columns, and added a one-time destructive reset for existing v1 data. | Updated Phase 3 plan, development design, README, and CONTEXT. |
| 2026-07-12 | `persistence/database.py`, `tests/test_phase3_ibapi.py` | Extended `processed_1m_bar` schema v3 with raw request metadata columns `bar_size`, `what_to_show`, `use_rth`, and `source`; existing v1/v2 data is cleared once because the schema is not migrated. | Updated Phase 3 plan, development design, README, and CONTEXT. |
| 2026-07-12 | Phase 3 timestamp naming rule | Clarified that persisted database timestamp columns do not use the `_et` suffix; `_et` is reserved for runtime America/New_York aware datetimes, while raw IBAPI `date` remains a UTC epoch value. | Updated Phase 3 plan, development design, and CONTEXT. |
| 2026-07-12 | `persistence/database.py`, `tests/test_phase3_ibapi.py` | Rebuilt `processed_1m_bar` as schema v4 and added a one-time destructive reset for v1/v2/v3 databases; the current processed-bar table is forbidden from containing columns with an `_et` suffix. | Updated Phase 3 plan, development design, README, and CONTEXT. |
| 2026-07-12 | `persistence/database.py`, `tests/test_phase3_ibapi.py` | Added `processed_1m_bar.timestamp` as the America/New_York bar timestamp at `bar_size` precision while retaining raw UTC epoch `date`; schema v5 clears incompatible v1-v4 data once. | Updated Phase 3 plan, development design, README, and CONTEXT. |
| 2026-07-12 | `persistence/database.py`, `application/single_day_runner.py`, `tests/test_phase3_ibapi.py` | Added per-run processed-bar CSV export at `data/<run_id>.csv`; CSV fields and order are derived from `processed_1m_bar`, while SQLite persistence remains unchanged. | Updated Phase 3 plan, development design, README, and CONTEXT. |
