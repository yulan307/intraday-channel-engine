# Revision History

| 2026-07-17 | Channel prediction engine, parameter CSV, SQLite/CSV audit persistence, and Channel tests | Added required `curr_mix_ratio`, separate last/current prediction audit fields, normalized sigmoid blending, and forward-compatible processed-bar column migration. Current predictions use the fixed delayed distance after `2 * delay`; first-segment final predictions remain null. | Updated README, CONTEXT, current development/flow/phase documentation, and this revision history. |

| 2026-07-17 | Phase 8 current-state documentation and `docs/archive/phase8_draft.md` | Consolidated the implemented Phase 8 design, flow, and plan into maintained current-state documents. Moved the historical decision draft to the ignored archive and removed it from Git tracking. | Updated development design, flow design, phase plan, and this revision history. |

| 2026-07-17 | `.gitignore`, `configs/ib_config_sample.yaml`, and Git tracking for `configs/ib.yaml` | Restricted tracked configuration artifacts to `*_sample.yaml` and `*_sample.csv`; added the IB profile sample and made the actual IB profile local-only. | Updated README and CONTEXT. |

| 2026-07-17 | `run_live.ps1`, `run_backtest.ps1` | Changed launcher YAML selection to an optional first filename under `configs/`; the scripts internally pass the corresponding Python `--config` argument and preserve remaining CLI overrides. | Updated README and CONTEXT. |

| 2026-07-16 | `live_cli.py`, IB gateway, SQLite persistence, Windows mutex support, Live YAML/configs, and Phase 8 tests | Implemented independent different-symbol Live Paper runs: private run databases, per-process client IDs with 326 retry, symbol/merge mutexes, completed-run atomic source-wins merge, processed CSV export, and retention on incomplete/failure paths. SQLite initialization is now non-destructive for all callers. | Updated README, CONTEXT, Phase 8 draft, and current-state design/flow/phase-plan documents. |

| 2026-07-15 | `engine/decision_engine.py`, `application/bar_processor.py`, and Decision/Phase 3 tests | Replaced effective-trend gating with current-Bar slope gating. BUY requires `trend_slope >= trend_slope_std`; SELL requires `trend_slope <= -trend_slope_std`. Signal post-processing remains unchanged. | Updated README, CONTEXT, current Decision Engine flow/development documentation, and the phase plan. |

| 2026-07-15 | `tests/test_live_cli_status.py` | Made the invalid-date CLI regression test derive its temporary configuration from the tracked live YAML sample and use the tracked parameter-set sample, avoiding dependence on local runtime configuration files. | Sample YAML and CSV files were not modified. |

| 2026-07-15 | `engine/channel_engine.py` and Channel engine tests | Added delayed oldest-prefix selection for `curr_*` calculation. The delay is `trend_window // 2`; after the warmup ranges, the calculation uses `channel_stack[:n-delay]` while the stack remains capped by `channel_window`. | Updated CONTEXT and current Channel flow/development documentation. |

| 2026-07-15 | `engine/decision_engine.py`, `application/bar_processor.py`, and Decision/Phase 3 tests | Added effective-trend gating to BUY/SELL generation. BUY now also requires UP or SIDEWAY; SELL requires DOWN or SIDEWAY. Existing signal post-processing remains unchanged. | Updated README, CONTEXT, and current Decision Engine flow/development documentation. |

| 2026-07-15 | `domain/parameters.py`, Trend/Channel engines, SQLite persistence, parameter CSV templates, and tests | Replaced obsolete `slope_std_window`, `dev_window`, and `residual_window` fields with required `channel_window`. Trend now uses `trend_window` for both regression and valid-slope history; Channel retains only its latest `channel_window` Bars. Advanced the incompatible SQLite schema to `backtest_run_statistics_v2`. | Updated CONTEXT, current-state flow/development design documents, and this revision history. |

| 2026-07-13 | `application/bar_processor.py`, `persistence/database.py`, `tests/test_phase3_expand.py`, and `tests/test_phase3_ibapi.py` | Auto Threshold signal updates now reset Trend and Channel state for the following Bar while preserving the signal Bar calculation. `processed_1m_bar.decision` now persists `NULL` for no action and only `BUY` or `SELL` for triggered signals; the schema was advanced to `phase3_expand_v2`. | Updated `docs/phase3_expand.md`, `README.md`, `CONTEXT`, and this revision history. |

| Date       | Changed files / modules | Summary | Related documentation updates |
|------------|--------------------------|---------|-------------------------------|
| 2026-07-14 | `support/logging.py`, IBAPI gateway, Live feed, runners, CLIs, configs, and tests | Added required INFO/ERROR YAML log levels, terminal-mirrored startup-through-first-Bar diagnostics, complete IBAPI error callback recording and live-request error propagation. Normal processing continues silently after the first confirmed Bar; Live starts a five-minute terminal heartbeat at that boundary. | Updated README, CONTEXT, Phase 3/5 current-state documents, and this revision history. |
| 2026-07-14 | `run_backtest.ps1`, `run_live.ps1` | Added root-level Windows launchers that use the project virtual environment, set the project working directory, and forward CLI help and overrides unchanged. | Updated README, CONTEXT, Phase 3/5 current-state docs, and this revision history. |
| 2026-07-14 | `src/single_day_test/bar_feed/live_paper_feed.py`, `tests/test_phase4_live_feed.py` | Enforced a 60-second minimum historical request duration so a keep-up-to-date 1-minute request started exactly at session open remains compatible with IBKR. Added a regression test for the session-open boundary. | Updated README, CONTEXT, Phase 4 current-state documents, and this revision history. |
| 2026-07-14 | `application/backtest_cli.py`, `configs/backtest.yaml`, Phase 3 tests | Replaced JSON-request and subcommand backtest startup with the sole YAML-first `backtest_cli`; merged scanner orchestration into that module, added field-level CLI overrides, and removed obsolete JSON configs and `backtest_scan.py`. | Updated `README.md`, `CONTEXT`, `docs/phase3_expand.md`, and this revision history. |
| 2026-07-12 | Phase 0 `src/` package, `.gitignore`, and tests | Added the project/package layout and git ignore rules; implemented Phase 0 domain enums, errors, parameters, models, states, and pure Protocol interfaces. Added ET-aware datetime normalization with naive-datetime rejection, `FeedEvent` validation, and unit coverage. No database, IBAPI/TWS, algorithm, or order implementation was added. | Updated the phase plan, flow, and development design documents, plus `CONTEXT`, to record the Phase 0 contracts. |
| 2026-07-12 | `engine/regression.py`, `engine/trend_engine.py`, `engine/channel_engine.py`, `engine/decision_engine.py`, `tests/test_engine.py`, and `pyproject.toml` | Implemented Phase 1's IO-free regression, Trend, Channel, and Decision algorithms. Added NumPy as a runtime dependency and unit coverage for regression validation, trend windows/classification, channel transition/prediction semantics, and Decision post-persistence state transitions. | Updated `README.md`, `CONTEXT`, and this revision history to reflect the implemented Phase 1 scope. |
| 2026-07-12 | Phase 2 persistence, feed, application, and CLI modules | Added SQLite schema/repositories, local BacktestFeed validation, atomic processed-bar/signal persistence, run summaries, and a JSON-request Backtest CLI. Raw Bar import and Raw Bar E2E tests are intentionally deferred until user-provided data is injected. | Updated README and CONTEXT for the Phase 2 offline workflow. |
| 2026-07-12 | Phase 3 `ib/`, canonical raw-bar model, SQLite storage, CLI, and tests | Replaced Phase 2 injected raw data with the canonical IBAPI BarData shape (UTC epoch date, OHLC, volume, WAP, and bar count). Added callback-correlated TWS gateway, schedule and historical-bar services, local-cache-first fetch flow, and one-time legacy-schema rebuild with later fail-fast detection. | Updated README, CONTEXT, Phase 3 plan/development documentation, and this revision history. |
| 2026-07-12 | `configs/ib.yaml`, IB configuration loader, and run CLI | Replaced environment-variable TWS connection configuration with YAML paper/live profiles selected by the CLI. | Updated README, CONTEXT, and this revision history. |
| 2026-07-12 | IBAPI dependency and gateway version guard | Replaced the obsolete PyPI ibapi 9.81 dependency with the official IBKR API requirement and added a fail-fast protocol check for SCHEDULE support. | Updated README, CONTEXT, and this revision history. |
| 2026-07-12 | `IbApiGateway.error` callback | Updated the error callback to the official API 10.48 signature, including `errorTime`, so TWS errors are converted into domain errors instead of terminating the event thread. | Updated this revision history. |
| 2026-07-12 | SQLite processed-bar persistence | Corrected the processed-bar INSERT placeholder count to match the Phase 3 table schema. | Updated this revision history. |
| 2026-07-12 | `configs/parameter_set.csv`, parameter loader, and run request format | Centralized parameter definitions in CSV and reduced request JSON to a CSV path plus `parameter_set_id`; embedded parameter dictionaries are no longer accepted. | Updated README, CONTEXT, and this revision history. |
| 2026-07-12 | `persistence/database.py` | Corrected the processed-bar INSERT placeholder count discovered during the first injected-data Backtest. The failed transaction wrote no processed bars or signals. | Revision history updated with the production validation correction. |
| 2026-07-12 | `persistence/database.py`, SQLite `raw_1m_bar` and `processed_1m_bar` | Expanded persisted Trend, Channel, and Decision result JSON fields into queryable columns, retained JSON for compatibility, and added Raw Bar source fields `bar_size`, `what_to_show`, and `use_rth`. Existing processed rows are backfilled during schema initialization. | Revision history updated for the schema migration. |
| 2026-07-12 | `persistence/database.py`, SQLite timestamp columns | Renamed persisted `timestamp_et` fields to `timestamp`; timestamp values remain ET-normalized by the domain layer. Existing database tables are migrated in place during initialization. | Revision history updated for the timestamp naming convention. |
| 2026-07-12 | `persistence/database.py`, SQLite `processed_1m_bar` | Added Raw Bar source metadata columns to processed bars, backfilled existing runs from raw storage, and removed redundant Trend/Channel/Decision JSON columns after their values had been expanded. | Updated CONTEXT and revision history for the normalized persisted shape. |
| 2026-07-12 | `domain/models.py`, `application/bar_processor.py`, `persistence/database.py`, `tests/test_phase3_ibapi.py` | Rebuilt `processed_1m_bar` as Phase 3 schema v2: preserved all RawBar fields including `wap` and `barCount`, expanded parameter/Trend/Channel/Decision payloads into dedicated columns, removed JSON columns, and added a one-time destructive reset for existing v1 data. | Updated Phase 3 plan, development design, README, and CONTEXT. |
| 2026-07-12 | `persistence/database.py`, `tests/test_phase3_ibapi.py` | Extended `processed_1m_bar` schema v3 with raw request metadata columns `bar_size`, `what_to_show`, `use_rth`, and `source`; existing v1/v2 data is cleared once because the schema is not migrated. | Updated Phase 3 plan, development design, README, and CONTEXT. |
| 2026-07-12 | Phase 3 timestamp naming rule | Clarified that persisted database timestamp columns do not use the `_et` suffix; `_et` is reserved for runtime America/New_York aware datetimes, while raw IBAPI `date` remains a UTC epoch value. | Updated Phase 3 plan, development design, and CONTEXT. |
| 2026-07-12 | `persistence/database.py`, `tests/test_phase3_ibapi.py` | Rebuilt `processed_1m_bar` as schema v4 and added a one-time destructive reset for v1/v2/v3 databases; the current processed-bar table is forbidden from containing columns with an `_et` suffix. | Updated Phase 3 plan, development design, README, and CONTEXT. |
| 2026-07-12 | `persistence/database.py`, `tests/test_phase3_ibapi.py` | Added `processed_1m_bar.timestamp` as the America/New_York bar timestamp at `bar_size` precision while retaining raw UTC epoch `date`; schema v5 clears incompatible v1-v4 data once. | Updated Phase 3 plan, development design, README, and CONTEXT. |
| 2026-07-12 | `persistence/database.py`, `application/single_day_runner.py`, `tests/test_phase3_ibapi.py` | Added per-run processed-bar CSV export at `data/<run_id>.csv`; CSV fields and order are derived from `processed_1m_bar`, while SQLite persistence remains unchanged. | Updated Phase 3 plan, development design, README, and CONTEXT. |
| 2026-07-12 | `support/ids.py`, `application/backtest_cli.py`, `tests/test_ids.py` | Changed CLI run identifiers from UUIDs/request overrides to `YYYYMMDD-HHMMSS_symbol_parameter_set_id_3-random-alphanumeric-characters`, using the America/New_York program start time truncated to seconds. | Updated the flow design, development design, Phase 3 plan, README, and CONTEXT. |
| 2026-07-12 | `application/backtest_cli.py`, `support/ids.py` | Changed the `run_id` timestamp component from America/New_York time to the local machine time, retaining one-second precision. | Updated the flow design, Phase 3 plan, README, and CONTEXT. |
| 2026-07-12 | Phase 3 scanner, threshold state, SQLite schema, CLI, parameter CSV, request JSON files, and tests | Added active parameter-set scanning across inclusive date ranges, generated per-parameter-set run IDs, daily `COMPLETED`/`FAILED`/`SKIPPED` records, one multi-day CSV export, and Fixed/Auto Threshold behavior. Rebuilt incompatible SQLite tables without migration; processed bars retain the v5 field order except for removed `initial_threshold` and nullable current `active_threshold`. | Updated Phase 3 Expand, phase plan, README, and CONTEXT. |
| 2026-07-14 | `bar_feed/live_paper_feed.py`, `persistence/database.py`, and `tests/test_phase4_live_feed.py` | Changed the live historical lookback margin from 1800 seconds to 10 seconds. Treated an IBKR-provided prior-session final RTH Bar as an ignored callback boundary only when it is the first initial historical callback and precedes the resolved session. Enabled the SQLite connection for serialized IBAPI callback-thread raw-bar persistence. All other session-external Bars remain errors. | Updated the Phase 4 draft, development design, README, CONTEXT, and this revision history. |
| 2026-07-14 | `docs/phase5_draft.md` | Added the Phase 5 draft, comparing the completed Phase 4 bar-feed contract with the Phase 5 closed-loop target and documenting the remaining Live entrypoint, run lifecycle, persistence, failure handling, observability, and acceptance-test gaps. | Added the new Phase 5 current-state planning document. |
| 2026-07-14 | `docs/phase5_draft.md` | Archived the seven confirmed Phase 5 decisions: Live CLI entrypoint, run ID format, non-persisted live-phase transition, signal eligibility for all completed-bar sources, transactional persistence, failure handling, and deferred real-TWS acceptance. | Updated the Phase 5 decision archive. |
| 2026-07-14 | `docs/phase5_draft.md` | Archived three additional Phase 5 decisions: create the run before pre-market waiting, use one injected clock for all Phase 5 time, and make `LivePaperFeed` own deadline-aware waiting. | Updated the Phase 5 decision archive. |
| 2026-07-14 | `docs/phase5_draft.md` | Simplified the Phase 5 live-phase decision: `live_phase` remains `NULL` and does not affect run behavior or persistence transitions. | Updated the Phase 5 decision archive. |
| 2026-07-14 | Live CLI, runner, feed waiting, terminal persistence, JSONL logging, and Phase 5 tests | Implemented the Phase 5 single-day Live Paper closed loop. The Live CLI creates a fixed-threshold run before pre-market waiting, the runner consumes Phase 4 completed Bars using one injected clock, Feed owns deadline-aware waits, and terminal summary/status writes are atomic. Added per-run JSONL audit logs and deterministic Live integration/failure coverage. | Updated Phase 5 current-state documentation, README, CONTEXT, phase plan, and revision history. |
| 2026-07-14 | `configs/live_config.yaml`, `application/live_cli.py`, and Live config tests | Added YAML-first Phase 5 startup configuration for symbol, direction, fixed threshold, parameter selection, IB environment, and optional trade date. Explicit CLI values override only their matching YAML fields. | Updated README, CONTEXT, Phase 5 draft, phase plan, and revision history. |
| 2026-07-14 | `application/threshold_policy.py`, `application/bar_processor.py`, live/backtest CLIs, startup confirmation support, configs, and tests | Auto Threshold now initializes from the first completed Bar raw open in both backtest and Live Paper; Live accepts null or omitted thresholds as Auto. Both CLIs print validated effective launch configuration and require Enter before runtime side effects. | Updated README, CONTEXT, Phase 3/4/5 and phase-plan documentation, and this revision history. |
| 2026-07-14 | Threshold policy, live/backtest configuration, run context, SQLite persistence, and tests | Added `threshold_update_rate`: a validated 0-100 percent Auto signal-update adjustment. BUY subtracts the rate and SELL adds it; null or omitted values resolve to zero. Each run persists the effective rate in `single_day_run`; the schema advanced to `phase3_expand_v3`. | Updated README, CONTEXT, Phase 3/5 and phase-plan documentation, and this revision history. |
| 2026-07-14 | Live/backtest launch configuration, threshold-mode selection, tests, and documentation | A numeric threshold plus an explicitly numeric `threshold_update_rate`, including zero, now selects Auto and uses the threshold as the initial active value. Null or omitted rate keeps numeric thresholds Fixed. Startup confirmation now explicitly reports `auto_threshold_enabled`. | Updated README, CONTEXT, Phase 3/5 and phase-plan documentation, and this revision history. |
| 2026-07-14 | `application/live_cli.py` and Live CLI status tests | Treated Live CLI input validation as an expected exit with concise console output, JSONL audit logging, and exit code 2. Added date-selection and tiered pre-market wait status reporting to both console and JSONL logs. | Updated README, CONTEXT, Phase 5 draft, phase plan, and revision history. |
| 2026-07-14 | `configs/*_sample.*`, `.gitignore`, local runtime configuration policy | Added tracked backtest, Live, and parameter-set configuration templates. Local `backtest.yaml`, `live_config.yaml`, `parameter_set.csv`, and all generated `data/` files are now ignored and remain runtime-local. | Updated `README.md`, `CONTEXT`, and this revision history. |
| 2026-07-14 | `bar_feed/live_paper_feed.py`, `application/live_cli.py`, and `tests/test_phase4_live_feed.py` | Changed late or duplicate completed live Bars after emission from terminal ordering errors to logged, skipped events. The feed records the complete raw Bar and ordering context, then continues processing later valid Bars. | Updated the Phase 4 draft, development design, CONTEXT, and this revision history. |

| 2026-07-14 | `docs/ibapi_single_day_test_development_design.md`, `docs/ibapi_single_day_test_flow_design.md`, `docs/ibapi_single_day_test_phase_plan.md`, removed `docs/phase3_expand.md`, `docs/phase4_draft.md`, and `docs/phase5_draft.md` | Consolidated the implemented Phase 3 Expand, Phase 4 Live Paper feed, and Phase 5 single-day Live Paper closed-loop state into the three maintained design documents. Synchronized schema v3, YAML-first startup, Auto Threshold, logging and validation behavior, session waiting, IBAPI callback handling, atomic terminal persistence, and logged/skipped late or duplicate Bars. Removed the superseded draft documents from the maintained documentation set. | Updated the three maintained design documents and this revision history. |
| 2026-07-15 | IB configuration/gateway, Live CLI/order submitter, Live feed/runner, configurations, and Phase 7 tests | Implemented Live-only virtual-account market-order submission. IB profiles now use independent market and order client IDs; Live validates ordered share quantities, discovers one managed account, submits accounted `MKT`/`DAY`/`SMART`/`USD` orders with reconnect handling, and consumes a quantity only after a normal local submission return. Source classification now occurs in the Runner at consumption time, with LIVE-only eligibility and post-order SQLite state advancement. | Updated README, CONTEXT, Phase 7 current-state design/flow/plan documents, configuration comments, tests, and this revision history. |
| 2026-07-15 | SQLite schema/repositories, Backtest scanner, single-day runner, summaries, and focused tests | Added ET-aware raw-Bar timestamps, daily threshold/signal/reward statistics, and one scan-level `run_summary` per run ID. Backtest aggregation continues after failed dates; Live writes the compatible one-day statistics. Incompatible local SQLite schemas are rebuilt as `backtest_run_statistics_v1`. | Updated README, CONTEXT, all three maintained design documents, and this revision history. |
| 2026-07-15 | Runtime state, summaries, SQLite repositories, Backtest scanner, and focused tests | Moved daily statistics to runtime accumulation, changed reward to a 0-1 normalized best-price proximity score, added scan-level maximum daily metrics, and made Backtest keep processed records in memory for one CSV per run ID instead of SQLite `processed_1m_bar` rows. Live retains processed-Bar SQLite auditing and the same daily statistics. | Updated README, CONTEXT, all three maintained design documents, and this revision history. |
| 2026-07-15 | `application/summary_service.py` and reward summary tests | Changed `best_reward` to `min(best_price / best_order_price, best_order_price / best_price)` and return null reward/efficiency when either price is zero. | Updated README, CONTEXT, all three maintained design documents, and this revision history. |
| 2026-07-16 | `application/live_cli.py`, `application/single_day_runner.py`, `bar_feed/live_paper_feed.py`, IB gateway/order submitter, persistence schema, and Live recovery tests | Added same-run Live recovery: five-minute completed-Bar timeout, connection-status logging without immediate subscription cancellation, pre-market gateway closure, market-open reconnection, session-start replay with processed-Bar upsert, persisted recovery count, and persisted `share` / `remained_shares` snapshots. Recovery waits 20 seconds, 1 minute, 15 minutes, then 1 hour until session close; SQLite write failures are terminal. | Updated README, CONTEXT, current Live design/flow/plan documents, recovery document, and this revision history. |
| 2026-07-16 | `bar_feed/live_paper_feed.py` and Live feed timeout tests | Fixed the first historical-callback timeout for an in-session subscription: it now starts from the later of session start and subscription start, preventing immediate recovery timeouts during session-start replay. | Updated README, CONTEXT, current Live design/flow/plan documents, and this revision history. |
