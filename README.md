# Intraday Channel Engine – Phase 0

Phase 0 implements the project skeleton, domain models, enums, errors, parameters, runtime states, and pure Protocol interfaces.

**No algorithms, no database, no IBAPI/TWS, no orders, no recovery.**

See `docs/` for design documents and `CONTEXT` for current state.
